# Agent Notes - Global

- **Web search preference**: use `web_search` for simple lookups; switch to `MiniMax_web_search` (via `mcp()`) for research chains that benefit from `related_searches` and date metadata.
